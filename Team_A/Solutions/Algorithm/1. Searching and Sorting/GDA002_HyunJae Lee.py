#User function Template for python3

def chocolates (arr, n) : 
    #Complete the function
    index = len(arr)
    while index <= len(arr):
        arr.remove(max(arr))
        index -=1
        return arr
        if index = 1
